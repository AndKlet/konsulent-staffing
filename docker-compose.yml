version: '3.8'

services:
  konsulent-api:
    build: ./konsulent-api
    ports:
      - "8000:8000"
    
  llm-verktoy-api:
    build: ./llm-verktoy-api
    ports:
      - "8001:8001"
    depends_on:
      - konsulent-api
    env_file:
      - llm-verktoy-api/.env