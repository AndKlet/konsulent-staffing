version: '3.8'

services:
  konsulent-api:
    build: ./konsulent-api
    ports:
      - "8000:8000"
    
  llm-verktøy-api:
    build: ./llm-verktøy-api
    ports:
      - "8001:8001"
    depends_on:
      - konsulent-api